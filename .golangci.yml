version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - misspell

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true

issues:
  exclude-rules:
    - path: main\.go
      linters: [gocritic]
