linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - gofmt
    - misspell

linters-settings:
  gofmt:
    simplify: true

issues:
  exclude-rules:
    # exit calls in main are intentional
    - path: main\.go
      linters: [gocritic]
